// All frontend demo data initialization removed;
// All exported functions now return nothing or null, ensuring 0 UI data.

export function initializeDemoData() {
  // No demo data; do nothing
}

export const dataStore = {
  getUser: () => null,
  getUserById: () => null,
  updateUserPassword: () => false,
  // ...add other empty or null-returning dataStore functions as needed
};
